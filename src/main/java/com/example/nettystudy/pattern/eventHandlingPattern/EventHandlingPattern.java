package com.example.nettystudy.pattern.eventHandlingPattern;

/**
 * packageName : com.example.nettystudy.pattern.eventHandlingPattern
 * fileName : EventHandlingPattern
 * author : taeil
 * date : 2023/08/21
 * description :
 * =======================================================
 * DATE          AUTHOR                      NOTE
 * -------------------------------------------------------
 * 2023/08/21        taeil                   최초생성
 */
public class EventHandlingPattern {
    // 이벤트 핸들링 패턴
    // 주요 동작
    // 이벤트 핸들링을 위한 객체들을 초기화(Intiate)하고,
    // 여러 통로에서 들어오는 이벤트들을 수신(recieve)하고,
    // 이벤트들을 대응할 객체별로 분할하고(demultiplex),
    // 개체에게 이벤트를 발송(dispatch)해서,
    // 그 개체가 이벤트에 걸맞는 작업을 수행(process events)한다.
    // 이 일련의 동작으로 동시다발적으로 들어오는 각각의 입력에 대해서 적절한 대응을 수행할 수 있다.
    // 다양한 방법으로 이 추상적인 패턴을 구현하는 것이 가능하다.
}